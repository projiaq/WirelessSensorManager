# 已知问题

- 尚未使用真实接收器和传感器验证；自动化测试不能替代射频、距离、断电和 bootloader 测试。
- Silicon Labs DFU 设备依赖广播 AD type `0x1B` 携带原设备地址；需要对量产 bootloader 实测。
- MAC 写入与 EM4 会按固件设计主动断链，因此不能执行普通配置的即时回读；App 会提示重新扫描确认。
- 临时包名仍为 `com.example.wirelesssensormanager`，发布前必须确认正式企业包名。

- 尚未使用真实接收器和传感器验证，不能视为量产验收。
- 传感器测点编号需人工选择；固件不提供出厂编号。
- 压力工程单位未冻结，界面只使用固件原始整数单位。
- 接收器 39 字节 Notify 依赖足够 MTU，固件没有分片降级协议。
- 无法从单个传感器判断它是否已配置在另一台接收器中。
- 包名 `com.example.wirelesssensormanager` 是临时包名，发布前必须确认正式企业包名。
- 传感器 MAC 修改和 OTA 未开放。
